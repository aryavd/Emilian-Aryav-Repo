from datasets import DatasetDict


def load_dataset(dataset_name: str) -> DatasetDict:
    """
    Load a dataset from its name.
    Args:
        dataset_name: Name of the dataset

    Returns:
        DatasetDict where one can access the `train` and `test` sets.
    """
    return None
